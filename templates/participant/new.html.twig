{% extends 'base.html.twig' %}

{% block title %}New Participant{% endblock %}

{% block body %}
    <h1>Create new Participant</h1>

    {{ include('participant/_form.html.twig') }}

    <a href="{{ path('participant_index') }}" class="btn btn-success">back to list</a>
{% endblock %}
